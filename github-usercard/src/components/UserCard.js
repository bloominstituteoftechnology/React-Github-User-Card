import React from "react";

export default function UserCard(props) {
    const {
        avatar_url,
        id,
        login,
        html_url
    } = props.user;

    return(
        <div>

        </div>
    )
}